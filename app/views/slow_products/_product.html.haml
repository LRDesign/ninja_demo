= content_tag_for :tr, product do
  %td= product.name 
  %td= product.description 
  %td= product.price 
  %td= link_to 'Show', deleteable_product_path(product) 
  %td= delete_widget product.name, deleteable_product_path(product)
