= content_tag_for :tr, product do
  %td= product.name 
  %td= product.description 
  %td= product.price 
  %td= link_to 'Show', slow_product_path(product) 
  %td= link_to 'Edit', edit_slow_product_path(product) 
  %td= link_to 'Destroy', slow_product_path(product), :confirm => 'Are you sure?', :method => :delete